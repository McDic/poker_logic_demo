# Rust
/target
**/*.rs.bk

# Node
/web/node_modules
/web/dist
/web/.vite
/web/*.tsbuildinfo

# Generated WASM bindings (rebuilt via scripts/build-wasm.sh)
/web/src/wasm/*
!/web/src/wasm/.gitkeep

# Editor / OS
.DS_Store
.idea
.vscode

# Python (pyenv virtualenv pin — project is not Python)
.python-version
